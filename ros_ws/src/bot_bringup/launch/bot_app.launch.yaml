%YAML 1.2
---
launch:
  - arg:
      name: "namespace"
      default: "ifacello"

  - group:
    - push-ros-namespace:
        namespace: "$(var namespace)"
    - node:
        pkg: "control_service"
        exec: "graph"
        name: "graph_node"
